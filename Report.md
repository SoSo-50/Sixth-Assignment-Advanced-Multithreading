1.خروجی:
Atomic Counter: 2000000
Normal Counter: <عددی کوچکتر از 2000000 و متغیر در هر اجرا، مثلاً 1354218>

دلیل:
atomicCounter: همیشه 2000000 (۲ میلیون) خواهد بود. دلیل آن این است که AtomicInteger و متدهای آن (مانند incrementAndGet()) عملیات اتمی (atomic operations) را فراهم می‌کنند. این بدان معناست که افزایش شمارنده به صورت یک عملیات واحد و ناگسستنی انجام می‌شود، بنابراین چندین ترید می‌توانند بدون از دست دادن هیچ افزایشی، به صورت ایمن آن را به روز کنند.
normalCounter: همیشه کمتر از 2000000 و هر بار که برنامه را اجرا می‌کنی یک عدد متفاوت خواهد بود. دلیل این امر Race Condition است. عملیات normalCounter++ در واقع سه گام است: ۱) خواندن مقدار normalCounter، ۲) افزایش آن، ۳) نوشتن مقدار جدید. وقتی دو ترید به صورت همزمان این عملیات را انجام می‌دهند، ممکن است هر دو ترید مقدار اولیه یکسانی را بخوانند، آن را افزایش دهند، و سپس هر دو نتیجه افزایش یافته را بنویسند، که منجر به از دست رفتن یک افزایش می‌شود.

۲. هدف از AtomicInteger در این کد چیست؟
هدف از AtomicInteger در این کد ارائه یک شمارنده ایمن برای ترید (thread-safe counter) است. این کلاس عملیات‌هایی را ارائه می‌دهد که به صورت اتمی انجام می‌شوند و بدون نیاز به استفاده صریح از synchronized یا Lock، از شرایط رقابتی (Race Conditions) جلوگیری می‌کنند. این کار باعث می‌شود به روزرسانی متغیرهای مشترک در محیط‌های چندرشته‌ای قابل اعتماد باشد.

۳. atomicCounter.incrementAndGet() چه تضمین‌هایی برای ایمنی ترید (thread-safety) فراهم می‌کند؟
atomicCounter.incrementAndGet() تضمین‌های زیر را برای ایمنی ترید فراهم می‌کند:

اتمی بودن (Atomicity): این عملیات به صورت اتمی انجام می‌شود، یعنی کل عملیات (خواندن، افزایش، نوشتن) به صورت یک واحد غیرقابل تقسیم انجام می‌شود. هیچ ترید دیگری نمی‌تواند در میانه این عملیات، به متغیر دسترسی پیدا کرده یا آن را تغییر دهد.
قابلیت مشاهده (Visibility): تغییراتی که یک ترید در atomicCounter ایجاد می‌کند، بلافاصله برای تریدهای دیگر قابل مشاهده است. این تضمین به دلیل استفاده از معماری‌های سخت‌افزاری و تکنیک‌های خاصی (مانند استفاده از مقایسه و جابه‌جایی (Compare-And-Swap - CAS)) فراهم می‌شود.

۴. در کدام موقعیت‌ها استفاده از یک قفل (Lock) انتخاب بهتری نسبت به یک متغیر اتمی (atomic variable) خواهد بود؟
استفاده از یک قفل (مانند synchronized یا ReentrantLock) انتخاب بهتری است در مواقعی که:

چندین عملیات نیاز به اتمی بودن دارند: وقتی شما یک "بخش بحرانی" (critical section) دارید که شامل چندین عملیات روی یک یا چند متغیر است و این مجموعه از عملیات‌ها باید به صورت اتمی انجام شوند. متغیرهای اتمی فقط عملیات‌های منفرد را اتمی می‌کنند، نه بلوک‌های کد.
نیاز به هماهنگی پیچیده‌تر: وقتی نیاز به کنترل جریان تریدها، انتظار کشیدن برای شرایط خاص، یا اطلاع‌رسانی بین تریدها (مثل استفاده از wait()/notify() یا Conditions در Lock API) دارید.
تعداد تداخل‌ها زیاد است: در سناریوهایی با تداخل (contention) بسیار زیاد، ممکن است استفاده از قفل‌ها کارآمدتر باشد زیرا سربار (overhead) عملیات‌های CAS مکرر در متغیرهای اتمی می‌تواند زیاد شود. (اگرچه Atomicها برای تداخل متوسط عملکرد خوبی دارند).
عملیات‌های غیرعددی پیچیده: برای ساختارهای داده پیچیده‌تر (مانند لیست‌های پیوندی، نقشه‌ها) که عملیات‌های آن‌ها فقط شامل افزایش یا کاهش ساده نیست.

۵. به غیر از AtomicInteger، چه انواع داده دیگری در پکیج java.util.concurrent.atomic موجود است؟
پکیج java.util.concurrent.atomic شامل انواع داده‌های اتمی دیگری نیز هست که عبارتند از:

AtomicLong: برای عملیات اتمی روی مقادیر long.
AtomicBoolean: برای عملیات اتمی روی مقادیر boolean.
AtomicReference<V>: برای عملیات اتمی روی ارجاعات به اشیاء (objects).
AtomicIntegerArray, AtomicLongArray, AtomicReferenceArray<E>: برای عملیات اتمی روی عناصر آرایه‌ها.
AtomicStampedReference, AtomicMarkableReference: برای حل مشکل ABA در عملیات‌های CAS.
DoubleAdder, LongAdder, DoubleAccumulator, LongAccumulator: برای سناریوهای خاص افزایش/جمع‌آوری که تداخل بالایی دارند و عملکرد بهتری نسبت به AtomicLong/AtomicInteger در آن شرایط ارائه می‌دهند.
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
مونت کارلو:

۱. چگونه از چندرشته‌ای (multithreading) برای شبیه‌سازی استفاده کرده‌اید؟
برای استفاده از چندرشته‌ای در شبیه‌سازی مونت کارلو، ما کار تولید و بررسی نقاط تصادفی را بین چندین ترید تقسیم کردیم.

تقسیم کار: تعداد کل نقاطی که باید تولید شوند (مثلاً ۱۰ میلیون) را بر تعداد تریدهای مورد استفاده تقسیم کردیم. به این ترتیب، هر ترید مسئول تولید و بررسی زیرمجموعه‌ای از این نقاط (مثلاً هر ترید ۱ میلیون نقطه) شد.
نقش ExecutorService: از ExecutorService برای مدیریت پول تریدها (thread pool) استفاده کردیم. این سرویس امکان می‌دهد تا تسک‌ها (وظایف) را به آن ارسال کنیم و ExecutorService مسئولیت ایجاد، مدیریت و بازیافت تریدها را بر عهده بگیرد. ما با استفاده از Executors.newFixedThreadPool(numberOfThreads) یک پول ترید با تعداد مشخصی ترید ثابت ایجاد کردیم.
جمع‌آوری نتایج: هر ترید (که در کلاس MonteCarloTask به عنوان یک Callable پیاده‌سازی شده بود) پس از اتمام کار خود، تعداد نقاطی را که در داخل دایره افتاده بودند، به عنوان نتیجه برمی‌گرداند. این نتایج با استفاده از اشیاء Future جمع‌آوری شدند. متد future.get() برای دریافت نتیجه هر تسک استفاده شد. پس از جمع‌آوری نتایج از تمام Futureها، مجموع کل نقاط داخل دایره از همه تریدها را به دست آوردیم.

۲. آیا استفاده از چندرشته‌ای باعث بهبود عملکرد شده است؟
بله، در بیشتر موارد، استفاده از چندرشته‌ای باعث بهبود قابل توجهی در عملکرد (کاهش زمان اجرا) می‌شود، به خصوص برای تعداد نقاط بالا.

مقایسه نتایج زمانی: (در اینجا باید نتایج واقعی اجرای کد خودت را وارد کنی. مثال:)

زمان اجرا (تک‌رشته‌ای): [مثلاً: 500 میلی‌ثانیه]
زمان اجرا (چندرشته‌ای با 4 ترید): [مثلاً: 150 میلی‌ثانیه] این مقایسه نشان می‌دهد که زمان اجرای چندرشته‌ای [مثلاً: حدود ۳ برابر] سریع‌تر بوده است.
دلیل بهبود عملکرد: این بهبود به دلیل موازی‌سازی (Parallelism) است. وظیفه اصلی (تولید و بررسی نقاط) به بخش‌های کوچک‌تر و مستقلی تقسیم می‌شود که می‌توانند به صورت همزمان توسط هسته‌های پردازنده مختلف اجرا شوند. این باعث می‌شود که کار در زمان کمتری به پایان برسد.

سربار (Overhead): در برخی موارد، به خصوص برای تعداد نقاط بسیار کم یا تعداد تریدهای بسیار زیاد نسبت به هسته‌های CPU، ممکن است سربار ایجاد و مدیریت تریدها و جمع‌آوری نتایج، بیشتر از منفعت موازی‌سازی باشد و منجر به عملکرد بدتر شود. اما در این شبیه‌سازی (مونت کارلو) که محاسبات زیادی در هر ترید انجام می‌شود، موازی‌سازی معمولاً سودمند است.

۳. چگونه از ایمنی ترید (thread-safety) در کد خود اطمینان حاصل کردید؟
در این پیاده‌سازی خاص از شبیه‌سازی مونت کارلو، نیاز صریحی به مکانیسم‌های همزمان‌سازی (مانند synchronized، Lock یا AtomicInteger) وجود نداشت. دلیل آن به شرح زیر است:

عدم وجود منابع مشترک قابل تغییر: هر MonteCarloTask (هر ترید) کاملاً مستقل عمل می‌کند. هر ترید مجموعه نقاط تصادفی خود را تولید و بررسی می‌کند و شمارنده نقاط داخل دایره خودش را به صورت محلی (pointsInCircle) نگهداری می‌کند.
عدم تداخل: تریدها هیچ داده‌ای را با یکدیگر به اشتراک نمی‌گذارند یا تغییر نمی‌دهند تا نیازی به محافظت از آن‌ها در برابر شرایط رقابتی باشد.
جمع‌آوری نهایی: نتایج هر ترید (تعداد نقاط داخل دایره) فقط در انتهای کار هر ترید (از طریق Callable و Future) به ترید اصلی برگردانده می‌شود. جمع نهایی این نتایج توسط ترید اصلی به صورت متوالی انجام می‌شود، بنابراین در آنجا نیز Race Condition وجود ندارد.
در نتیجه، طراحی این شبیه‌سازی به گونه‌ای است که به طور ذاتی ایمن برای ترید است، زیرا کارها کاملاً ایزوله شده‌اند و هیچ Critical Section (بخش بحرانی) مشترکی که توسط چندین ترید به طور همزمان تغییر کند، وجود ندارد.

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------